import pool from "./../config/db.js";

// Fetch a paper with a particular id from the DB
// as well as the paper's text from the retrieved tuple's url field 
export async function fetchPaperById(id) {}

// Fetch recommendations based on a paper with a particular id
export async function fetchRecommendations(id) {}
