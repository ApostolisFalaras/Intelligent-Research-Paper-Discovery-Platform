import PaperCard from "./PaperGrid.jsx";

function PaperGrid({ papers }) {
    if (papers.length === 0) {}


    return (
        <div className="paper-grid">

        </div>
    );
}

export default PaperGrid;